using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DusterControl : Overlap_Generic {


	// Update is called once per frame
	void Update () {
		if(overlap && Input.GetKeyDown(KeyCode.E))
		{

		}	
	}
}
